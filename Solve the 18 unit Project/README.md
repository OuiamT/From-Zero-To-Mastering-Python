# 📬 Personalized Letter Generator:
This project automatically generates personalized invitation letters using Python. It reads a list of names and a letter template, replaces placeholders, and creates ready-to-send text files for each invited person.

## 📌 Project Description:
- Instead of writing the same letter multiple times, this script:
  - Reads a template letter
  - Reads a list of invited names
  - Replaces placeholders with real data
  - Creates a separate personalized letter for each name
- This project is a practical example of:
  - File handling in Python
  - String replacement
  - Automation of repetitive tasks
